#include <bits/stdc++.h>
using namespace std;

const int maxn = 1005;
int N, K;

int main()
{
	freopen("data.in", "r", stdin);
	
	scanf("%d%d", &N, &K);
	int w;
	string name1, name2;
	for (int i = 0; i < N; i++) {
		cin >> name1 >> name2 >> w;
		
	}
}
